<mvc:View
    controllerName="sap.zen.dsh.rsrt.controller.Chart"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:viz="sap.viz.ui5.controls"
    xmlns:viz.feeds="sap.viz.ui5.controls.common.feeds"
    xmlns:viz.data="sap.viz.ui5.data"
    xmlns:m="sap.m"
    xmlns:core="sap.ui.core"
    id="preview-chart"
    class="sapUiZenCommonsSize">
  <viz:VizFrame
      id="idVizFrame"
      selectData="onChartPress"
      uiConfig="{applicationSet:'fiori'}"
      vizType="{om>/dataProvider/0/Chart/chartType}"
      renderComplete="adjustHeight"
      width="100%"
      height="100%">
    <viz:dataset>
      <viz.data:FlattenedDataset
          id="flatDS"
          data="{om>/dataProvider/0/FlatResultSet}"
          dimensions="{om>/dataProvider/0/Chart/Dimensions}"
          measures="{om>/dataProvider/0/Chart/Measures}"
          >
        <viz.data:dimensions>
          <viz.data:DimensionDefinition
              id="chartViewDimDef"
              name="{om>Description}"
              identity="{om>Name}"
              value="{om>Name}" />
        </viz.data:dimensions>
        <viz.data:measures>
          <viz.data:MeasureDefinition
              id="chartViewMeasureDef"
              name="{om>Description}"
              identity="{om>Name}"
              value="{om>Name}" />
        </viz.data:measures>
      </viz.data:FlattenedDataset>
    </viz:dataset>

  </viz:VizFrame>
  <m:VBox
      id="vbMessages"
      items="{om>/dataProvider/0/Chart/Messages}"
      visible="true">
    <m:MessageStrip
        id="msgstrip"
        text="{om>msgText}"
        showCloseButton="false"
        showIcon="false"
        type="Information" />
  </m:VBox>
</mvc:View>
